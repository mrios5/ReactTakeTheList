const LaLista = (student) => {
    var list = []
    list.push(student)
    return list
}

export default LaLista

/*
    if(list.indexOf(student)){
        console.log(list)
        return list
    }else {
        list.push(student)
        return list
    }
     */